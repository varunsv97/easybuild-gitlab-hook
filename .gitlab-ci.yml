stages:
  - generate
  - build

variables:
  EB_PATH: "/data/rosi/shared/eb/easybuild"
  EB_SOURCE: "/bigdata/rz/sudhar46/easybuild_manual_test/codebase/eb"
  SOURCE_PATH: "/data/rosi/shared/eb/easybuild_source"
  ARCH: 'skylake'
  PARTITION: 'cpu-skylake'


default:
  tags: ['rosi-admin-shell']
  before_script:
    - ml ${ARCH}
    - ml python
    - source $EB_SOURCE/bin/activate
  id_tokens:
    CI_JOB_JWT:
      aud: "https://codebase.helmholtz.cloud"

generate_pipeline:
  stage: generate
  script:
    - echo "Generating EasyBuild GitLab CI pipeline for GPU packages"
    - echo "DEBUG Environment variables:"
    - echo "GITLAB_CI_GENERATE=$GITLAB_CI_GENERATE"
    - echo "Current directory is $(pwd)"
    - ls -la gitlab_ci_hook.py || echo "Hook file NOT FOUND"
    - |
      eb --hooks=gitlab_ci_hook.py \
         --installpath=${EB_PATH}/${ARCH} \
         --installpath-modules=${EB_PATH}/${ARCH}/modules \
         --tmp-logdir=eblog \
         --buildpath=ebbuild \
         --sourcepath=/data/rosi/shared/eb/easybuild_source \
         --robot --insecure-download --disable-mpi-tests --ignore-test-failure --fix-deprecated-easyconfigs \
         --robot-paths=${CI_PROJECT_DIR}/easyconfigs \
         --ignore-checksums --module-naming-scheme=CategorizedHMNS \
         --accept-eula-for=Intel-oneAPI,CUDA,NVHPC,cuDNN \
         --trace --max-parallel=8 \
         JupyterLab-4.0.3-GCCcore-12.2.0.eb
         JupyterLab-4.0.5-GCCcore-12.3.0.eb
         JupyterLab-4.2.0-GCCcore-13.2.0.eb
         JupyterLab-4.2.5-GCCcore-13.3.0.eb
         JupyterLab-4.4.4-GCCcore-14.2.0.eb
         JupyterLab-4.4.9-GCCcore-14.3.0.eb


    - echo "Pipeline generation completed"
    - cat easybuild-child-pipeline.yml


  artifacts:
    paths:
      -  easybuild-child-pipeline.yml
    expire_in: 1 week

execute_builds:
  stage: build
  trigger:
    include:
      - artifact:  ${CI_PROJECT_DIR}/easybuild-child-pipeline.yml
        job: generate_pipeline
    strategy: depend
  variables:
    PARENT_PIPELINE_ID: $CI_PIPELINE_ID
    SCHEDULER_PARAMETERS: "--nodes=1 --ntasks-per-node=8 --partition=${PARTITION} --mem=200G"